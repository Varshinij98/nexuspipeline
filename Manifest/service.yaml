apiVersion: v1
kind: Service
metadata:
  name: mycontainer
spec:
  selector:
    matchLabels:
      app: mycontainer
  ports:
    - protocal: TCP
      port: 80
      targetPort: 8086
      
