apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: tomcat-helm
  name: tomcat-helm
spec:
  replicas: 2
  selector:
    matchLabels:
      app: tomcat-helm
  template:
    metadata:
      labels:
        app: tomcat-helm
    spec:
      containers:
      - image: 8184848757/mycontainer:latest
        name: tomcat-9
